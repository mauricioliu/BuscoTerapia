<%= form_for(@ref_datum) do |f| %>
  <% if @ref_datum.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ref_datum.errors.count, "error") %> prohibited this ref_datum from being saved:</h2>

      <ul>
      <% @ref_datum.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br />
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :valor %><br />
    <%= f.text_field :valor %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
