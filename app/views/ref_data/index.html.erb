<h1>Listing ref_data</h1>

<table>
  <tr>
    <th>Nombre</th>
    <th>Valor</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @ref_data.each do |ref_datum| %>
  <tr>
    <td><%= ref_datum.nombre %></td>
    <td><%= ref_datum.valor %></td>
    <td><%= link_to 'Show', ref_datum %></td>
    <td><%= link_to 'Edit', edit_ref_datum_path(ref_datum) %></td>
    <td><%= link_to 'Destroy', ref_datum, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Ref datum', new_ref_datum_path %>
